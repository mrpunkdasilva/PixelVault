# Server Configuration
PORT=3001

# Path Configuration
# These paths are relative to the 'server' directory.
DATA_PATH=./data
UPLOADS_PATH=./uploads

# Security
# A strong, random string used for signing JWTs.
# This MUST be kept secret in production.
JWT_SECRET=
